#!/usr/bin/env bash
#for i in $(seq 1 10); do
while :; do
    password=$(op read 'op://Personal/Spotify/password')
    result=$?
    if [[ $result -eq 0 ]]; then
        echo "$password"
        break
    else
        sleep 1
    fi
done
