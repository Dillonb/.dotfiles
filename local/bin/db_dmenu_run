#!/bin/bash

if [[ -x /usr/bin/rofi ]]; then
    dmenu_path | rofi -dmenu "$@" | ${SHELL:-"/bin/sh"} &
else
    if [[ -x /usr/bin/yegonesh ]]; then
        yegonesh
    else
        dmenu_run
    fi
fi
